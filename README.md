# FlowLingo

AI communication platform built on a smart keyboard.

## Structure

- `backend/`: Go API server (auth, translation, analysis, subscriptions)
- `ios/`: iOS app + keyboard extension (incoming)
- `android/`: Android app + keyboard service (incoming)
- `site/`: Marketing website + admin dashboard (incoming)
- `architecture/`: Architecture documents (incoming)

## Backend Setup

See `backend/README.md` for instructions.
